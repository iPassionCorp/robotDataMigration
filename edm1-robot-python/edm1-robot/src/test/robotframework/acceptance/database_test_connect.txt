*** Settings ***
Suite Setup
Suite Teardown
Library           DatabaseLibrary
Library           Selenium2Library
Library           String
Library           OperatingSystem
Library           Collections
Resource          ../keywords/edm1_sit.txt

*** Test Cases ***
connect to databse
    connect to EDM1    #Connect to postgres
    ${result}=    Row Count    SELECT a.appno FROM dm.cert a INNER JOIN dm.certmapping b ON a.certno = b.certno INNER JOIN tlp.customerinfo c ON b.rcertno = c.certificateno WHERE a.appno <> '000000000000'    0
    log    ${result}

*** Keywords ***
